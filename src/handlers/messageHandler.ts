import type { ClientMessage } from "../types/messages.js";

export const messageHandler = (message: ClientMessage) => {
  switch (message.type) {
    case 'reg':
      
      break;
  
    default:
      break;
  }
}